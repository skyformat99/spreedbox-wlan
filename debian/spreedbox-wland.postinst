#!/bin/sh

set -e

case "$1" in
    configure|triggered)
        if [ -e "/etc/init/spreedbox-wland.conf" ]; then
            service spreedbox-wland restart || true
        fi
        ;;
    *)
        exit 0
    ;;
esac

#DEBHELPER#
